VITE_PUBLIC_URL=https://your-github-user.github.io/your-repo/

# Optional: only change these if the default PeerJS cloud is blocked for you.
# VITE_PEER_SERVER={"host":"0.peerjs.com","port":443,"path":"/","secure":true}

# Optional: JSON array of STUN/TURN servers. The project already ships with
# public STUN plus OpenRelay TURN defaults for cross-network play.
# VITE_ICE_SERVERS=[{"urls":["stun:stun.l.google.com:19302"]}]
